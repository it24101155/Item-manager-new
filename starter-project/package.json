{
  "name": "item-manager-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install": "cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm install && npm run build",
    "start": "cd backend && npm run dev"
  },
  "engines": {
    "node": ">=16"
  }
}
